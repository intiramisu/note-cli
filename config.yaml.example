# note-cli 設定ファイル
#
# このファイルを ~/.config/note-cli/config.yaml にコピーして使用してください:
#   mkdir -p ~/.config/note-cli
#   cp config.yaml.example ~/.config/note-cli/config.yaml
#
# または --config オプションで任意のパスを指定できます:
#   note-cli --config /path/to/config.yaml

# ------------------------------------------------------------------------------
# メモの保存先ディレクトリ
# ------------------------------------------------------------------------------
# メモファイル (.md) とタスクファイル (.tasks.yaml) が保存されるディレクトリ
# デイリーノートは <notes_dir>/daily/ に保存されます
# テンプレートは <notes_dir>/.templates/ に配置してください
#
# デフォルト: ~/notes
#
# notes_dir: ~/notes

# ------------------------------------------------------------------------------
# エディタ
# ------------------------------------------------------------------------------
# メモの作成・編集時に使用するエディタ
# $EDITOR 環境変数より優先されます
#
# 例: vim, nvim, nano, code, emacs
# デフォルト: vim
#
# editor: vim

# ------------------------------------------------------------------------------
# デフォルトタグ
# ------------------------------------------------------------------------------
# 新規メモ作成時に自動で付与されるタグ
# コマンドラインで -t オプションを指定した場合は、それに追加されます
#
# デフォルト: [] (空)
#
# default_tags:
#   - memo
#   - note-cli
